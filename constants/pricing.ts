/**
 * Flat delivery fee applied to every order, in ARS.
 * Single source of truth — previously duplicated between the checkout
 * summary and the demo order builder.
 */
export const DELIVERY_FEE = 1990;
